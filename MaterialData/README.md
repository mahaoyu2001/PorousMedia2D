# Material data

`Ceramic.txt` uses a simple whitespace-separated `key value` format. The
modeling workflow reads `Density`, Young's modulus `E`, and Poisson's ratio
`v`; the remaining entries are retained as provenance for the source material
parameter set. Units must be mutually consistent.
